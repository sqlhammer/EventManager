<Project>
  <PropertyGroup>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Judge spoke app. U5 ships a testable app-core (net10.0) + a MAUI Windows head.
         Android/iOS/Mac heads are a one-line TFM add once a JDK+Android SDK / Mac are available. -->
  </PropertyGroup>
</Project>
