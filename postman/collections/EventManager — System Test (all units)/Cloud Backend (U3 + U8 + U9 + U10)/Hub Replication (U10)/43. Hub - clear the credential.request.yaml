$kind: http-request
description: "US-802 / FD-Q8=B. The other half of refuse-rather-than-overwrite: clearing is an explicit, separate action, so a working credential cannot be destroyed by a careless paste."
url: "{{hubBaseUrl}}/api/replication/credential"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test('200 OK', () => pm.response.to.have.status(200));
    language: text/javascript
order: 13000
